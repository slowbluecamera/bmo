version: "1.0"
services:
  navidrome:
    image: deluan/navidrome:latest
    container_name: navidrome
    user: 1000:1000
    ports:
      - 4533:4533
    volumes:
      - /appdata/navidrome/data:/data
      - /mnt/bmo2/MUSIC:/music:ro 
    restart: unless-stopped
